@using System.ComponentModel.DataAnnotations

<PageTitle>@ClientData.PageTittle(ChildContent?.ToString() ?? ClientData.AppName)</PageTitle>

@code{
    [Required]
    [Parameter] 
    public RenderFragment? ChildContent { get; set; }
}
